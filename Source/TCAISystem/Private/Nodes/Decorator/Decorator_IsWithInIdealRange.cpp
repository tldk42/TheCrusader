// Written by jaegang lim


#include "Nodes/Decorator/Decorator_IsWithInIdealRange.h"

bool UDecorator_IsWithInIdealRange::CalculateRawConditionValue(UBehaviorTreeComponent& OwnerComp,
                                                               uint8* NodeMemory) const
{
	// if (OwnerController && OwnerController->GetPawn())
	// {
	// 	if (APawn* AIPawn = OwnerController->GetPawn())
	// 	{
	// 	}
	// }
	return false;
}
